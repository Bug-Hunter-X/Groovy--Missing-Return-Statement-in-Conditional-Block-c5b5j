```groovy
def myMethod(param) {
  if (param == null) {
    // Handle null input
    return defaultValue
  } else {
    // ... rest of the method
    return processedValue // Ensure you return something after processing!
  }
}
```